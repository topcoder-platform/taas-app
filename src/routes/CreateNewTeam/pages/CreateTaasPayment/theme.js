import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#229174",
    },
    error: {
      main: "#EF476F",
    },
    background: {
      default: "#ffffff",
    },
  },
});

export default theme;
